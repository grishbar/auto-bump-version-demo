name: Check version

on:
  pull_request:

jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: grishbar/auto-bump-version@main
        with:
          base-branch: main
